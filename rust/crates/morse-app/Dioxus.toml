[application]
name = "morse-app"

[web.app]
title = "Morse Learn"

[web.watcher]
reload_html = true

# The stylesheet and images are pulled in via the `asset!` macro in Rust, so no
# manual [web.resource] wiring is needed here.
